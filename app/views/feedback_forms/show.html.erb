<p id="notice"><%= notice %></p>
<div class="container">
  <div class="row">
    <div class="col-md-10 offset-1">
      <div class="card">
        <div class="card-header">
          <h2 class="text-center card-title"><%= @feedback_form.name %></h2>
        </div>
        <div class="card-body">
          <p class="lead"><%= @feedback_form.description %></h2>
        </div>
        <%= %>
      </div>
    </div>
  </div>
</div>
<%= link_to 'Edit', edit_feedback_form_path(@feedback_form) %> |
<%= link_to 'Back', feedback_forms_path %>
